[home]
pi ansible_host="{{ ip }}" ansible_user="{{ username }}" ansible_connection=ssh ansible_ssh_private_key_file="{{ ssh_key }}"
main_server ansible_host="{{ ip }}" ansible_user="{{ username }}" ansible_connection=ssh ansible_ssh_private_key_file="{{ ssh_key }}"

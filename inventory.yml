---
home:
  hosts:
    production:
      ansible_host: "{{ ip }}"
      ansible_user: "{{ username }}"
      ansible_connection: ssh
      ansible_ssh_private_key_file: "{{ ssh_key }}"
    staging:
      ansible_host: "{{ ip }}"
      ansible_user: "{{ username }}"
      ansible_connection: ssh
      ansible_ssh_private_key_file: "{{ ssh_key }}"
